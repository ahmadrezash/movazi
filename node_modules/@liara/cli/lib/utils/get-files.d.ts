/// <reference types="node" />
import { DebugLogger } from './output';
export interface IMapItem {
    data: Buffer;
    files: IFile[];
}
export interface IFile {
    checksum: string;
    path: string;
    size: number;
    mode: number;
}
export interface IDirectory {
    path: string;
    mode: number;
}
export default function getFiles(projectPath: string, debug?: DebugLogger): Promise<{
    files: IFile[];
    directories: IDirectory[];
    mapHashesToFiles: Map<string, IMapItem>;
}>;
