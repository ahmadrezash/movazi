"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const os_1 = tslib_1.__importDefault(require("os"));
const path_1 = tslib_1.__importDefault(require("path"));
const checkPath = (dir) => {
    if (!dir) {
        return;
    }
    const home = os_1.default.homedir();
    let location = '';
    dir = dir.replace(/\/+$/, ''); // trim end slashes
    const paths = {
        home,
        desktop: path_1.default.join(home, 'Desktop'),
        downloads: path_1.default.join(home, 'Downloads')
    };
    for (const locationPath of Object.keys(paths)) {
        if (dir === paths[locationPath]) {
            location = locationPath;
        }
    }
    if (!location) {
        return;
    }
    let locationName;
    switch (location) {
        case 'home':
            locationName = 'user directory';
            break;
        case 'downloads':
            locationName = 'downloads directory';
            break;
        default:
            locationName = location;
    }
    throw new Error(`You're trying to deploy your ${locationName}.`);
};
exports.default = checkPath;
